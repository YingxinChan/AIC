# Backend API base URL. Local dev doesn't need this set at all — it
# defaults to http://localhost:8000 (see src/lib/api.js). Production builds
# (Netlify, etc.) must set this to the deployed backend's real URL.
VITE_API_URL=http://localhost:8000
